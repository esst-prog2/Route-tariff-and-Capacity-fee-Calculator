# Planning Log

One line per decision: date — what was decided — who decided it (user or Claude). Earlier lines are never rewritten; new decisions are appended.

- 2026-09-22: Adopted this planning log convention — every project decision (requirement, number, name, tool) gets one appended line here with date, decision, and who decided it. Decided by: user.
- 2026-09-22: Installed Node.js 24.19.0 LTS (via winget) and OpenSpec CLI 1.13.0 (`@fission-ai/openspec` on npm; the plain `openspec` npm package is an unrelated squatted name), then ran `openspec init --tools claude` to scaffold spec-driven workflow files (`openspec/`, `.claude/`) in this repo. Decided by: user.
